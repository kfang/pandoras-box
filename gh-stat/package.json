{
  "name": "gh-stat",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "nx run-many -t build",
    "typecheck": "nx run-many -t typecheck",
    "clean": "nx run-many -t clean"
  },
  "devDependencies": {
    "nx": "^20.0.0"
  }
}
